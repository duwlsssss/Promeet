import { theme } from '@/styles/theme';

export const BACKWARD_ICON = {
  BACK: 'back',
  ARROW: 'arrow',
};

export const BUTTON_COLORS = {
  main: theme.color.main,
  point1: theme.color.point1,
};

export const BUTTON_TYPES = {
  button: 'button',
  submit: 'submit',
  reset: 'reset',
};
